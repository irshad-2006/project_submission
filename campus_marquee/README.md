# Campus Marquee

A standalone Streamlit app: a college event calendar with category filters,
favorites, and an admin mode for creating/deleting events.

## Run
```
pip install -r requirements.txt
streamlit run app.py
```

Data is stored in `data/events.json` and persists between runs.
