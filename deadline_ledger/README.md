# Deadline Ledger

A standalone Streamlit app for tracking assignments, priorities, and due dates
with reminder-threshold chips.

## Run
```
pip install -r requirements.txt
streamlit run app.py
```

Data is stored in `data/assignments.json` and persists between runs.
