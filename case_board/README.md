# Case Board

A standalone Streamlit app: a campus lost & found system with a weighted
matching engine that surfaces possible lost/found matches automatically.

## Run
```
pip install -r requirements.txt
streamlit run app.py
```

Data is stored in `data/case_board.json` and `data/case_board_meta.json`,
and persists between runs.
